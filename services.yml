apiVersion: apps/v1
kind: Service
metadata: 
 name: demo-app-service
 labels:
   app: demo-app
spec: 
  selector:
    app: demo-app
  ports:
  - port: 8080
    nodeport: 30008
    targetPort: 8080
  type: LoadBalancer
